{
  "$schema": "../../node_modules/wrangler/config-schema.json",
  "name": "your-worker-name",
  "main": "src/index.ts",
  "compatibility_date": "2026-04-01",
  "compatibility_flags": ["nodejs_compat"],
  "assets": {
    "directory": "../dashboard/dist",
    "html_handling": "auto-trailing-slash",
    "not_found_handling": "single-page-application",
    "run_worker_first": ["/api/*", "/pricing", "/favicon.ico"]
  },
  "d1_databases": [
    {
      "binding": "DB",
      "database_name": "aiusage-db",
      "migrations_dir": "migrations"
    }
  ],
  // 部署时通过 wrangler secret put 设置:
  // - SITE_ID
  // - ENROLL_TOKEN
  // - DEVICE_TOKEN_SECRET
  // - PROJECT_NAME_SALT
  "vars": {
    // Optional: set PUBLIC_HOST to your own custom hostname to redirect workers.dev.
    // Without it, the dashboard is served directly from this Worker.
    "MAX_DEVICES": "10",
    "PUBLIC_PROJECT_VISIBILITY": "masked",
    "DEFAULT_TIMEZONE": "Asia/Shanghai"
  }
}
